import type { JsTopicInterviewQa } from "../jsTopicTypes";

export const SCOPE_INTERVIEW_QA: JsTopicInterviewQa[] = [
  {
    id: "sc-1",
    question: "Чем отличается `var` от `let` и `const` по области видимости?",
    answer:
      "**`var`** объявляет переменную в **функции** (или глобальном объекте), **игнорируя блоки** `{ }`. **`let`** и **`const`** — **блочная** область: живут от `{` до соответствующей `}` (и в нескольких специальных конструкциях вроде `for`).",
  },
  {
    id: "sc-2",
    question: "Что такое hoisting («поднятие») в JS?",
    answer:
      "До выполнения кода движок **регистрирует объявления**: `var` и `function declaration` «видны» в начале своей области (с `undefined` для `var` до присваивания). **`let`/`const`/`class`** тоже поднимаются, но попадание к ним до строки объявления даёт **TDZ** (ReferenceError), а не `undefined`.",
  },
  {
    id: "sc-3",
    question: "Что такое Temporal Dead Zone (TDZ)?",
    answer:
      "Интервал от **входа в блок** до выполнения строки **`let`/`const`/`class`**, где идентификатор **существует в области**, но **обращаться** к нему нельзя — только `ReferenceError`. Например, `console.log(x); let x = 1;`.",
  },
  {
    id: "sc-4",
    question: "Почему в цикле `for (var i=0;...)` колбэки часто видят одно и то же `i`?",
    answer:
      "Одна переменная `i` на **всю** функцию; цикл успевает завершиться, и все колбэки читают уже **финальное** значение. С **`for (let i=0;...)`** на каждой итерации своя привязка `i` (для классического for).",
  },
  {
    id: "sc-5",
    question: "Можно ли изменить объект, объявленный через `const`?",
    answer:
      "**Ссылку** переприсвоить нельзя; **содержимое** объекта (поля) менять можно. `const` фиксирует привязку имени, не «глубокую неизменность».",
  },
  {
    id: "sc-6",
    question: "Что такое shadowing (затенение)?",
    answer:
      "Во **внутренней** области объявлено то же имя, что и снаружи: изнутри видна **внутренняя** переменная, внешняя **не доступна** по этому имени до выхода из блока.",
  },
  {
    id: "sc-7",
    question: "Есть ли своя область видимости у ES-модуля?",
    answer:
      "Да: **верхний уровень модуля** — не глобальная область скрипта в браузере; экспорт/импорт живут в **модульной** области. `var` на верхнем уровне модуля **не** становится свойством `window`.",
  },
  {
    id: "sc-8",
    question: "Чем область видимости отличается от замыкания?",
    answer:
      "**Область** — где **допустимо имя** переменной. **Замыкание** — функция **держит** доступ к лексическому окружению **снаружи**, даже после выхода из внешней функции. Замыкание строится **поверх** правил области.",
  },
  {
    id: "sc-9",
    question: "Объявление `function foo(){}` внутри блока — как ведёт себя в strict mode?",
    answer:
      "В **строгом** режиме и в модулях блочные **function declarations** ведут себя как **блочные** (в рамках блока). В **нестрогом** режиме исторически были отличия по движкам — на собесе безопаснее говорить про **strict/модуль** и не полагаться на странное поведение в слабом режиме.",
  },
  {
    id: "sc-10",
    question: "Почему `typeof possiblyUndeclared` безопасен, а `typeof x` перед `let x` — нет?",
    answer:
      "Для **необъявленного** идентификатора в определённых позициях `typeof` вернёт `undefined`. Для **`let x`** имя **уже в TDZ** — `typeof x` до объявления выбросит **ReferenceError**.",
  },
];
