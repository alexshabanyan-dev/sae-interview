import type { JsTopicInterviewQa } from "../jsTopicTypes";

export const EVENT_LOOP_INTERVIEW_QA: JsTopicInterviewQa[] = [
  {
    id: "el-1",
    question: "Что такое Event Loop и зачем он нужен в JavaScript?",
    answer:
      "JavaScript в одном потоке не может «ждать» сеть или таймер, блокируя весь UI. **Event Loop** — механизм среды (браузер, Node), который **снимает задачи с очередей** и выполняет колбэки, когда стек вызовов свободен. Синхронный код выполняется до конца; асинхронные части (таймеры, промисы, I/O) ставят работу в очереди, а цикл по очереди их подбирает.",
  },
  {
    id: "el-2",
    question:
      "Чем микрозадача отличается от макрозадачи? Приведи примеры.",
    answer:
      "**Макрозадачи** — например `setTimeout`/`setInterval`, I/O в Node, сообщения `postMessage` (упрощённо). За один «ход» цикла обычно берётся **одна** макрозадача. **Микрозадачи** — колбэки `Promise.then/catch/finally`, `queueMicrotask`, `MutationObserver` (браузер). После синхронного кода и **между** макрозадачами движок **опустошает всю** очередь микрозадач (пока в неё что-то не перестанут добавлять в том же проходе — на собесах это любимый нюанс).",
  },
  {
    id: "el-3",
    question: "Почему `Promise.resolve().then(...)` часто выполняется раньше, чем `setTimeout(fn, 0)`?",
    answer:
      "`then` планирует **микрозадачу**. `setTimeout(0)` — **макрозадачу**. Сначала выполняется весь текущий синхронный код, затем **все** микрозадачи из очереди, и только потом — следующая макрозадача. Поэтому колбэк промиса почти всегда раньше, чем таймер с нулевой задержкой.",
  },
  {
    id: "el-4",
    question: "Как `async/await` связан с микрозадачами?",
    answer:
      "После `await` **продолжение** async-функции не выполняется сразу: оно ставится в очередь как **микрозадача** (в терминах модели — «после разрешения ожидаемого значения»). Поэтому между строками до и после `await` может выполниться другой синхронный код и другие микрозадачи — отсюда классический порядок логов на собесах.",
  },
  {
    id: "el-5",
    question: "Что такое «зависание» из-за бесконечных микрозадач?",
    answer:
      "Если в обработчике микрозадачи снова и снова ставится **ещё одна** микрозадача (например, `queueMicrotask` или `then`, который снова планирует `then`), макрозадачи (таймеры, отрисовка, ввод) могут **не получить хода** долгое время — интерфейс «подвисает». На собесе это проверяют как понимание приоритета микроочереди.",
  },
  {
    id: "el-6",
    question: "В чём главное отличие Event Loop в браузере и в Node.js (в двух словах)?",
    answer:
      "Идея та же (очереди + стек), но **фазы и источники задач** разные: в Node есть **libuv**, фазы `timers`, `poll`, `check` (`setImmediate`), `nextTick` (отдельная очередь с **ещё более высоким** приоритетом, чем промисы — частая ловушка). В браузере важны **рендер** и **микрозадачи** относительно `requestAnimationFrame`. Детали спрашивают реже, но «в Node порядок может отличаться из-за `nextTick`/`setImmediate`» — хороший ответ.",
  },
  {
    id: "el-7",
    question: "Можно ли считать `requestAnimationFrame` микро- или макрозадачей?",
    answer:
      "В спеках это **отдельный** механизм привязки к кадру; в шпаргалке для собеса его не смешивают с `then`. Обычно говорят: **перед следующей отрисовкой** браузер вызывает rAF-колбэки по своим правилам; точный порядок относительно микрозадач лучше не заучивать, а знать, что это **не то же самое**, что `setTimeout` или `Promise`.",
  },
];
