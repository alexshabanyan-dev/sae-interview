import type { JsTopicInterviewQa } from "../jsTopicTypes";

export const COERCION_INTERVIEW_QA: JsTopicInterviewQa[] = [
  {
    id: "co-1",
    question: "Чем `==` отличается от `===`?",
    answer:
      "**`===`** (строгое равенство) сравнивает **без приведения типов**: разные типы → сразу `false` (кроме особого случая `NaN`). **`==`** (нестрогое) при разных типах **пытается привести** операнды к общему виду по правилам спецификации, затем сравнивает.",
  },
  {
    id: "co-2",
    question: "Перечисли все «ложные» (falsy) значения в JS.",
    answer:
      '`false`, `0`, `-0`, `0n` (BigInt ноль), "" (пустая строка), `null`, `undefined`, `NaN`. Всё остальное в булевом контексте — **truthy**.',
  },
  {
    id: "co-3",
    question: "Почему `[] == false` даёт `true`?",
    answer:
      "При `==` массив приводится к примитиву через `ToPrimitive` → строка `''` для пустого массива; `''` при сравнении с `boolean` даёт число `0`; `0 == false` после приведения `false` к `0` — **true**.",
  },
  {
    id: "co-4",
    question: "Чем `Number.isNaN` отличается от глобального `isNaN`?",
    answer:
      "**`isNaN`** сначала **приводит** аргумент к числу (`Number(x)`), поэтому `isNaN('oops')` — `true`. **`Number.isNaN`** проверяет: значение **уже** число **и** оно NaN — без приведения; `Number.isNaN('oops')` — `false`.",
  },
  {
    id: "co-5",
    question: "Что делает `Object.is` и чем отличается от `===`?",
    answer:
      "В основном как `===`, но: **`Object.is(NaN, NaN)` — `true`**, а **`Object.is(0, -0)` — `false`**. У `===` наоборот: `NaN !== NaN`, а `0 === -0`.",
  },
  {
    id: "co-6",
    question: "Почему `parseInt('08')` в старых браузерах могло быть проблемой?",
    answer:
      "Если **не передать radix** `10`, `parseInt` мог интерпретировать строку с ведущим нулём как **восьмеричную**. Сейчас в ES5+ ведущий `0` без `0x` не ведёт к octal для `parseInt` в строгом смысле — но на собесе ждут ответ: **всегда указывай второй аргумент `10`**.",
  },
  {
    id: "co-7",
    question: "Что вернёт `[] + []` и почему?",
    answer:
      "Строка `''` (пустая). Оба массива при `+` приводятся к строкам через `ToString`; пустой массив → `''`; конкатенация двух пустых строк.",
  },
  {
    id: "co-8",
    question: "Зачем использовать `!!value`?",
    answer:
      "Привести любое значение к **настоящему** `boolean`: первый `!` даёт противоположный boolean, второй — обратно. Удобно для явного флага без `Boolean()`.",
  },
];
