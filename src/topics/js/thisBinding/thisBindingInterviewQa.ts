import type { JsTopicInterviewQa } from "../jsTopicTypes";

export const THIS_BINDING_INTERVIEW_QA: JsTopicInterviewQa[] = [
  {
    id: "th-1",
    question: "Как в JavaScript определяется значение `this`?",
    answer:
      "У **обычных функций** `this` задаётся **вызовом** (не местом объявления): неявная привязка (`obj.method()`), явная (`call`/`apply`/`bind`), через `new`, либо в strict при прямом `f()` — **`undefined`**, в non-strict sloppily иногда глобальный объект. У **стрелочных функций** своего `this` нет — берётся **лексически** из внешней области.",
  },
  {
    id: "th-2",
    question: "Почему `const g = obj.m; g()` теряет `this`?",
    answer:
      "При `g()` вызов идёт **без базового объекта** — это обычный вызов функции, не метод. Неявная привязка к `obj` не применяется. Нужно `g.call(obj)`, `obj.m.bind(obj)` или обёртка `() => obj.m()`.",
  },
  {
    id: "th-3",
    question: "Чем отличается `call`, `apply` и `bind`?",
    answer:
      "`call(thisArg, a, b, …)` и `apply(thisArg, [a, b, …])` **сразу** вызывают функцию с заданным `this`. `bind(thisArg, …)` возвращает **новую** функцию с **зафиксированным** `this` (и опционально частично применёнными аргументами), вызов позже.",
  },
  {
    id: "th-4",
    question: "Как `new` влияет на `this` в конструкторе?",
    answer:
      "При `new Fn()` создаётся новый объект; **`this` внутри `Fn`** ссылается на этот объект (если не вернули другой объект явно). Если конструктор возвращает **объект** — он подменяет результат `new`; примитив игнорируется.",
  },
  {
    id: "th-5",
    question: "Почему стрелка в `{ m: () => this.x }` не видит `x` с объекта?",
    answer:
      "Стрелка **не** получает `this` от объекта-литерала; она захватывает `this` **снаружи** (модуль, класс, функция), где была **создана**. Для метода, который должен видеть экземпляр/объект, используют обычный `m() { … }`, либо поле-класса со стрелкой.",
  },
  {
    id: "th-6",
    question: "Что будет с `this` в методе, переданном как колбэк в `addEventListener`?",
    answer:
      "В современных браузерах обработчик обычно вызывается с **`this` = элемент**, к которому подписались (как при `listener.call(element, event)`). Если нужен другой контекст — `bind` или стрелка (тогда `this` лексический). На собесе иногда уточняют именно поведение DOM.",
  },
  {
    id: "th-7",
    question: "Как в классе сохранить `this` для метода, переданного наружу?",
    answer:
      "Варианты: **`bind`** в конструкторе или при передаче, **поле со стрелкой** `m = () => { … }` на экземпляре, или явная обёртка. Классовые **обычные** методы при `const { m } = obj; m()` снова теряют `this` — как у объектного литерала.",
  },
];
