import type { JsTopicInterviewQa } from "../../js/jsTopicTypes";

export const PAGE_LOAD_INTERVIEW_QA: JsTopicInterviewQa[] = [
  {
    id: "pl-1",
    question:
      "Что происходит после Enter в адресной строке — в одном предложении и чуть подробнее?",
    answer:
      "Браузер **инициирует навигацию**: разбирает URL, при необходимости узнаёт IP через **DNS**, устанавливает **TCP** (или **QUIC** для HTTP/3), при **HTTPS** выполняет **TLS**, отправляет **HTTP-запрос**, получает ответ (возможна цепочка **редиректов**), парсит **HTML**, подтягивает **CSS/JS/картинки**, строит **DOM/CSSOM**, считает **layout**, **рисует** и **композитит** кадр. Подробности — в различии потоков и того, что блокирует парсер.",
  },
  {
    id: "pl-2",
    question: "Зачем DNS, если мы уже знаем доменное имя?",
    answer:
      "Протоколы вроде TCP и HTTP работают с **IP-адресами**. Имя хоста (`example.com`) — для людей; **DNS** переводит имя в **A/AAAA** (или следует по **CNAME**). Без этого клиент не знает, куда открыть сокет.",
  },
  {
    id: "pl-3",
    question: "Чем отличается TCP handshake от TLS handshake?",
    answer:
      "**TCP** — надёжный байтовый поток: SYN/SYN-ACK/ACK, порты. **TLS** поверх TCP (или встроен в QUIC) — **шифрование**, выбор алгоритмов, **аутентификация сервера** сертификатом (и опционально клиента). Сначала обычно TCP, потом TLS Record Layer поверх него для HTTPS.",
  },
  {
    id: "pl-4",
    question: "Почему скрипт без async/defer блокирует парсинг HTML?",
    answer:
      "Классический **синхронный** `<script>` может вызвать `document.write` и менять разметку, поэтому парсер **останавливается**, качает и **исполняет** скрипт, затем продолжает. `defer` и `async` меняют порядок/момент выполнения; `type=module` по умолчанию ведёт себя как defer.",
  },
  {
    id: "pl-5",
    question: "Что такое reflow (layout) и repaint?",
    answer:
      "**Layout (reflow)** — пересчёт геометрии (где что лежит в координатах страницы). **Repaint** — отрисовка визуального вида без обязательного полного пересчёта дерева (часто после layout). Дорогие операции; избегают лишних чтений геометрии в цикле (forced synchronous layout).",
  },
  {
    id: "pl-6",
    question: "Что такое composite layers и зачем композитор?",
    answer:
      "Сложные эффекты (трансформы, opacity) могут выноситься на **отдельные слои** и анимироваться на **композиторном потоке** без полного пересчёта всей страницы. **GPU** помогает склеивать слои в финальный кадр.",
  },
  {
    id: "pl-7",
    question: "Что такое bfcache (back-forward cache)?",
    answer:
      "Когда уходишь со страницы **назад/вперёд**, браузер может **сохранить снимок** вкладки в памяти и восстановить мгновенно без полной перезагрузки. Не все страницы допускаются (есть ограничения из‑за WebSocket, unload handlers и т.д.).",
  },
  {
    id: "pl-8",
    question: "Где смотреть это в DevTools?",
    answer:
      "**Network** — тайминги запросов, водопад, размер. **Performance** — flame chart, Main thread vs Raster/Compositor. **Coverage**, **Rendering** (FPS, paint flashing). **Application** — SW, кэш. Для низкоуровневой цепочки также полезны **chrome://net-export** / встроенная диагностика (в зависимости от браузера).",
  },
  {
    id: "pl-9",
    question: "Как HTTP/2 и HTTP/3 меняют картину?",
    answer:
      "**HTTP/2** — мультиплексирование в **одном TCP**, приоритеты потоков, server push (редко). **HTTP/3** — **QUIC** поверх UDP, встроенный TLS 1.3, меньше head-of-line blocking при потере пакетов. Для «что после Enter» важно: один канал может нести много параллельных запросов без отдельного TCP на каждый ресурс (в отличие от классического HTTP/1.1 с лимитом соединений).",
  },
  {
    id: "pl-10",
    question: "Роль Service Worker в этой цепочке?",
    answer:
      "Если SW **зарегистрирован** и контролирует origin, он может **перехватить fetch** до сети и отдать ответ из **Cache Storage** или синтетический — тогда сеть вообще не дергается для этого запроса. Регистрация и активация — отдельные фазы жизненного цикла.",
  },
];
